// Loads media data from media.json (editable via the admin panel at
// /admin).
import mediaData from "./media.json";

export const mediaTypes = mediaData.types;
export const mediaItems = mediaData.items;